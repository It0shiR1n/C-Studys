#include <stdio.h>
#include <stdlib.h>

int main(){

    // malloc(sizeof(int)) -> Aloca um bloco de memória de tamanho especificado em bytes
    // calloc(10, sizeof(int)) -> Aloca memória de forma recursiva e especificada, inicializando todos os bytes com 0
    // realloc(void *ptr, size_t size) -> Realoca um bloco de memória existente
    
    // free(void *ptr) -> Libera um bloco de memória alocado por malloc ou calloc 

    

  
}